# Fixes del output (Specifications-28) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Marcadores de Anatomy como callouts (riel + línea); título de Layout como tag; distancia constante valor–línea de cota; ocultar cotas de dimensión de los hijos.

**Architecture:** `frames.ts` (mover `tagSeccion`), `anatomy.ts` (import del tag + rediseño de marcadores), `layout.ts` (tag, distancia de cota, quitar cotas de hijos).

**Tech Stack:** TypeScript, Figma Plugin API, esbuild, `npm test`.

---

## Nota de testing

Todo es impuro (toca `figma.*`) → sin tests unitarios nuevos; se verifica por PDF. Verificación
de cada tarea: `npm run build && npm test` sin errores ni regresiones (214 tests). Si al mover
`tagSeccion` o quitar `dibujarLineasHijos` queda algo sin uso, corregirlo en la misma tarea.

---

## Task 1: Mover `tagSeccion` a `frames.ts` y usarlo en Layout

**Files:**
- Modify: `src/plugin/generadores/frames.ts`
- Modify: `src/plugin/generadores/anatomy.ts`
- Modify: `src/plugin/generadores/layout.ts`

- [ ] **Step 1: Agregar `tagSeccion` en `frames.ts`**

En `src/plugin/generadores/frames.ts`, después de la función `chipVariable`, agregá:

```ts
// Chip con borde para el nombre de una sección (ej. "ANATOMY"): sin fill, stroke #374151.
export async function tagSeccion(etiqueta: string): Promise<FrameNode> {
  const chip = frameHorizontal("Tag", 0);
  chip.counterAxisAlignItems = "CENTER";
  chip.paddingTop = chip.paddingBottom = 6;
  chip.paddingLeft = chip.paddingRight = 16;
  chip.cornerRadius = 6;
  chip.fills = [];
  chip.strokes = [{ type: "SOLID", color: COLOR_VALOR }];
  chip.strokeWeight = 1;
  const t = await texto(etiqueta.toUpperCase(), 12, FONT_SEMI);
  t.fills = [{ type: "SOLID", color: COLOR_VALOR }];
  t.letterSpacing = { value: 8, unit: "PERCENT" };
  chip.appendChild(t);
  return chip;
}
```

(`COLOR_VALOR` = #374151 y `FONT_SEMI` ya existen en `frames.ts`.)

- [ ] **Step 2: Quitar `tagSeccion` local de `anatomy.ts` y importarla**

En `src/plugin/generadores/anatomy.ts`, eliminá la función local `tagSeccion` completa:

```ts
// Chip con borde para el nombre de la sección (ej. "ANATOMY"): sin fill, stroke #374151.
async function tagSeccion(etiqueta: string): Promise<FrameNode> {
  const chip = frameHorizontal("Tag", 0);
  chip.counterAxisAlignItems = "CENTER";
  chip.paddingTop = chip.paddingBottom = 6;
  chip.paddingLeft = chip.paddingRight = 16;
  chip.cornerRadius = 6;
  chip.fills = [];
  chip.strokes = [{ type: "SOLID", color: GRIS_OSCURO }];
  chip.strokeWeight = 1;

  const t = await texto(etiqueta.toUpperCase(), 12, FONT_SEMI);
  t.fills = [{ type: "SOLID", color: GRIS_OSCURO }];
  t.letterSpacing = { value: 8, unit: "PERCENT" };
  chip.appendChild(t);
  return chip;
}
```

Agregá `tagSeccion` al import desde `./frames.ts` (la línea que ya importa `frameVertical, frameHorizontal, texto, ...`). Luego, como `GRIS_OSCURO` y `FONT_SEMI` quedan sin uso en `anatomy.ts`:
- Eliminá la constante `const GRIS_OSCURO: RGB = { r: 0.216, g: 0.255, b: 0.318 };` y su comentario.
- Quitá `FONT_SEMI` del import de `./frames.ts` en `anatomy.ts` (verificá antes con búsqueda que no se use en otro lado del archivo; `FONT_BOLD` sí se mantiene).

- [ ] **Step 3: Usar el tag en `seccionDeLayout`**

En `src/plugin/generadores/layout.ts`, agregá `tagSeccion` al import desde `./frames.ts`
(la línea que ya importa `frameVertical, frameHorizontal, texto, ...`). Luego reemplazá:

```ts
  const seccion = frameVertical("Layout and Spacing", 64);
```

por:

```ts
  const seccion = frameVertical("Layout and Spacing", 24);
```

y reemplazá:

```ts
  seccion.appendChild(await texto("Layout and Spacing", 48));
```

por:

```ts
  seccion.appendChild(await tagSeccion("Layout and Spacing"));
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK (sin `GRIS_OSCURO`/`FONT_SEMI` sin uso en anatomy.ts); 214 tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/frames.ts src/plugin/generadores/anatomy.ts src/plugin/generadores/layout.ts
git commit -m "feat: título de Layout como tag (tagSeccion compartido en frames.ts)"
```

---

## Task 2: Marcadores de Anatomy como callouts (riel izquierdo + línea guía)

**Files:**
- Modify: `src/plugin/generadores/anatomy.ts` (`seccionDeAnatomy`)

- [ ] **Step 1: Agregar el helper de línea guía**

En `src/plugin/generadores/anatomy.ts`, antes de `seccionDeAnatomy`, agregá:

```ts
// Línea guía en L (del color del marcador) desde el marcador hasta el anchor del box:
// segmento horizontal a la altura del marcador + segmento vertical hasta el anchor.
function lineaGuia(artwork: FrameNode, desdeX: number, desdeY: number, haciaX: number, haciaY: number, color: RGB): void {
  if (haciaX !== desdeX) {
    const h = figma.createRectangle();
    h.x = Math.min(desdeX, haciaX);
    h.y = desdeY - 0.5;
    h.resize(Math.max(Math.abs(haciaX - desdeX), 0.01), 1);
    h.fills = [{ type: "SOLID", color }];
    artwork.appendChild(h);
  }
  if (haciaY !== desdeY) {
    const v = figma.createRectangle();
    v.x = haciaX - 0.5;
    v.y = Math.min(desdeY, haciaY);
    v.resize(1, Math.max(Math.abs(haciaY - desdeY), 0.01));
    v.fills = [{ type: "SOLID", color }];
    artwork.appendChild(v);
  }
}
```

- [ ] **Step 2: Reservar el riel y centrar el clon dejándolo libre**

En `seccionDeAnatomy`, reemplazá el bloque del clon/canvas:

```ts
  // Margen para que los badges de las capas pegadas al borde no se corten.
  const MARGEN_ARTWORK = 20;
  // Tamaño mínimo del canvas gris: un elemento chico queda centrado en una caja amplia.
  const ARTWORK_MIN = 440;
  const clon = seleccionado.clone();
  artwork.appendChild(clon);
  const canvasW = Math.max(ARTWORK_MIN, clon.width + 2 * MARGEN_ARTWORK);
  const canvasH = Math.max(ARTWORK_MIN, clon.height + 2 * MARGEN_ARTWORK);
  artwork.resize(canvasW, canvasH);
  const offsetX = (canvasW - clon.width) / 2;
  const offsetY = (canvasH - clon.height) / 2;
  clon.x = offsetX;
  clon.y = offsetY;
```

por:

```ts
  // Margen para que los badges de las capas pegadas al borde no se corten.
  const MARGEN_ARTWORK = 20;
  // Riel a la izquierda del clon donde se apilan los marcadores (callouts).
  const RIEL = 80;
  // Tamaño mínimo del canvas gris: un elemento chico queda centrado en una caja amplia.
  const ARTWORK_MIN = 440;
  const clon = seleccionado.clone();
  artwork.appendChild(clon);
  const areaW = Math.max(ARTWORK_MIN, clon.width + 2 * MARGEN_ARTWORK);
  const canvasW = areaW + RIEL;
  const canvasH = Math.max(ARTWORK_MIN, clon.height + 2 * MARGEN_ARTWORK);
  artwork.resize(canvasW, canvasH);
  const offsetX = RIEL + (areaW - clon.width) / 2;
  const offsetY = (canvasH - clon.height) / 2;
  clon.x = offsetX;
  clon.y = offsetY;
```

- [ ] **Step 3: Reemplazar el loop de marcadores por el riel + líneas guía**

Reemplazá el loop actual:

```ts
  // Un marcador por elemento, posicionado sobre la caja real de la capa (clon centrado).
  const cajas = cajasRelativas(seleccionado);
  for (let i = 0; i < elementos.length; i++) {
    const caja = cajas.get(elementos[i].id);
    if (!caja) continue;
    const color = COLORES_MARCA[i % COLORES_MARCA.length];
    const x = caja.x + offsetX;
    const y = caja.y + offsetY;
    bordeMarca({ x, y, width: caja.width, height: caja.height }, color, artwork);
    artwork.appendChild(await marcador(i + 1, x - 8, y - 8, color));
  }
```

por (borde punteado en cada box + marcadores apilados en el riel con línea guía al anchor):

```ts
  // Borde punteado de cada box + recolección de anchors (esquina sup-izq del box).
  const cajas = cajasRelativas(seleccionado);
  const anchors: { numero: number; color: RGB; ax: number; ay: number }[] = [];
  for (let i = 0; i < elementos.length; i++) {
    const caja = cajas.get(elementos[i].id);
    if (!caja) continue;
    const color = COLORES_MARCA[i % COLORES_MARCA.length];
    const ax = caja.x + offsetX;
    const ay = caja.y + offsetY;
    bordeMarca({ x: ax, y: ay, width: caja.width, height: caja.height }, color, artwork);
    anchors.push({ numero: i + 1, color, ax, ay });
  }

  // Marcadores en el riel izquierdo, apilados de arriba a abajo (por Y del anchor),
  // separados para no superponerse; cada uno con una línea guía a su box.
  const RIEL_X = 16;
  anchors.sort((a, b) => a.ay - b.ay);
  let proximoTop = MARGEN_ARTWORK;
  for (const an of anchors) {
    const top = Math.max(an.ay - TAM_MARCADOR / 2, proximoTop);
    const cy = top + TAM_MARCADOR / 2;
    lineaGuia(artwork, RIEL_X + TAM_MARCADOR, cy, an.ax, an.ay, an.color);
    artwork.appendChild(await marcador(an.numero, RIEL_X, top, an.color));
    proximoTop = top + TAM_MARCADOR + 4;
  }
```

(La línea guía se dibuja antes del marcador para que el círculo quede por encima.)

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; 214 tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/anatomy.ts
git commit -m "feat: marcadores de Anatomy como callouts (riel izquierdo + línea guía)"
```

---

## Task 3: Distancia constante valor–línea de cota (4px)

**Files:**
- Modify: `src/plugin/generadores/layout.ts`

- [ ] **Step 1: Definir `SEP_VALOR` y usarlo en los callouts de spacing**

En `src/plugin/generadores/layout.ts`, cerca de las constantes `SEP_CHIP`/`FILA_TOP`, agregá:

```ts
const SEP_VALOR = 4; // separación constante entre el chip de la cota y su bracket
```

En `dibujarSpacingCallouts`, reemplazá:

```ts
    chip.x = xBr + 8; chip.y = y0 + largo / 2 - chip.height / 2;
```
por:
```ts
    chip.x = xBr + 6 + SEP_VALOR; chip.y = y0 + largo / 2 - chip.height / 2;
```

y reemplazá:

```ts
    chip.x = x0 + largo / 2 - chip.width / 2; chip.y = yBr + 8;
```
por:
```ts
    chip.x = x0 + largo / 2 - chip.width / 2; chip.y = yBr + 6 + SEP_VALOR;
```

- [ ] **Step 2: Usar `SEP_VALOR` en las cotas de W/H del elemento**

En `dibujarCotas`, reemplazá:

```ts
  tW.x = clon.x + clon.width / 2 - tW.width / 2;
  tW.y = clon.y - 44 - 12;
```
por:
```ts
  tW.x = clon.x + clon.width / 2 - tW.width / 2;
  tW.y = clon.y - 44 - SEP_VALOR - tW.height;
```

y reemplazá:

```ts
  tH.x = xLinea - tH.width - 2;
  tH.y = clon.y + clon.height / 2 - tH.height / 2;
```
por:
```ts
  tH.x = xLinea - SEP_VALOR - tH.width;
  tH.y = clon.y + clon.height / 2 - tH.height / 2;
```

- [ ] **Step 3: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests sin regresiones.

- [ ] **Step 4: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "fix: separación constante (4px) entre el valor de la cota y su línea"
```

---

## Task 4: Ocultar las cotas de dimensión de los hijos

**Files:**
- Modify: `src/plugin/generadores/layout.ts`

- [ ] **Step 1: Quitar el loop de medidas de hijos en `dibujarMarcas`**

En `dibujarMarcas`, eliminá este bloque (el comentario + `u` + el loop):

```ts
  // Medidas de hijos (Element measures): ancho al carril de arriba, alto al de la
  // izquierda, para que compartan la separación anti-colisión con el resto.
  const u = unidadActual();
  for (const h of hijos) {
    if (h.width !== clon.width) top.push({ c: await cota(etiquetaSpacing(h.width, u), COTA_DIM, artwork), centro: h.x + h.width / 2 });
    if (h.height !== clon.height) left.push({ c: await cota(etiquetaSpacing(h.height, u), COTA_DIM, artwork), centro: h.y + h.height / 2 });
  }
```

Y quitá el parámetro `hijos` de la firma. Reemplazá:

```ts
async function dibujarMarcas(artwork: FrameNode, marcas: Marca[], clon: FrameNode, hijos: Rect[] = []): Promise<number> {
```
por:
```ts
async function dibujarMarcas(artwork: FrameNode, marcas: Marca[], clon: FrameNode): Promise<number> {
```

- [ ] **Step 2: Eliminar `dibujarLineasHijos`**

Eliminá la función `dibujarLineasHijos` completa:

```ts
// Líneas de cota de ancho (arriba) y alto (izquierda) de cada hijo directo. Los
// badges (números) los ubica dibujarMarcas en los carriles externos.
function dibujarLineasHijos(artwork: FrameNode, hijos: Rect[], anchoPadre: number, altoPadre: number): void {
  for (const h of hijos) {
    if (h.width !== anchoPadre) {
      const cw = figma.createNodeFromSvg(svgCotaH("fixed", h.width));
      cw.x = h.x;
      cw.y = h.y - 14;
      artwork.appendChild(cw);
    }
    if (h.height !== altoPadre) {
      const ch = figma.createNodeFromSvg(svgCotaV("fixed", h.height));
      ch.x = h.x - 14;
      ch.y = h.y;
      artwork.appendChild(ch);
    }
  }
}
```

- [ ] **Step 3: Quitar las llamadas y `hijosMedidos` en `artworkModo`**

Eliminá estas dos líneas:

```ts
  const hijosMedidos = medirHijos ? hijosRects : [];
  if (medirHijos && modo !== "spacing") dibujarLineasHijos(artwork, hijosRects, clon.width, clon.height);
```

Reemplazá la llamada (rama GRID):

```ts
    const minLeftX = await dibujarMarcas(artwork, [], clon, hijosMedidos);
```
por:
```ts
    const minLeftX = await dibujarMarcas(artwork, [], clon);
```

Reemplazá la otra llamada (rama auto-layout):

```ts
  const minLeftX = await dibujarMarcas(artwork, [], clon, modo === "spacing" ? [] : hijosMedidos);
```
por:
```ts
  const minLeftX = await dibujarMarcas(artwork, [], clon);
```

Nota: el parámetro `medirHijos` de `artworkModo` queda sin uso (la opción ya no afecta las
cotas de dimensión). Dejalo en la firma para no propagar el cambio a `artworkDe`/`seccionDeLayout`/
`main.ts`; si el build se queja por variable/parámetro sin uso, prefijalo con `_medirHijos`.
No toques `hijosRects` ni el `rectOverlay` de hijos (los overlays se mantienen).

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK (sin `dibujarLineasHijos`, `hijosMedidos` ni `hijos` param sin uso); tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "fix: ocultar las cotas de dimensión de los hijos en el artwork"
```

---

## Verificación final (manual, por PDF)

Generar Anatomy + Layout y exportar PDF; confirmar:
- Anatomy: marcadores en el riel izquierdo, sin superposición, cada uno con su línea guía al box.
- Layout: el título es un tag con borde ("LAYOUT AND SPACING").
- Cotas con separación uniforme (~4px) entre el valor y la línea.
- Artwork de Layout sin las cotas de ancho/alto de hijos (solo W/H del elemento + padding/gaps).
