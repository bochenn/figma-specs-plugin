# Dark Mode del output — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un checkbox "Dark mode" que genera el output con texto claro + fondo oscuro del `Specifications`, dejando el artwork sin cambios, en las 8 secciones.

**Architecture:** Un módulo `tema` (light/dark) con estado. `texto()` aplica el color de texto del tema; `finalizar()` aplica el fondo. `main.ts` setea el tema antes de generar según el flag `dark` del mensaje. En light, el output queda idéntico al de hoy.

**Tech Stack:** TypeScript, esbuild, `@figma/plugin-typings`, `node --test`. Sin frameworks de UI.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---------|-----------------|
| `src/plugin/utils/tema.ts` | **Nuevo.** Paletas light/dark + `aplicarTema`/`temaActual`. |
| `src/plugin/generadores/frames.ts` | **Modificar.** `texto()` aplica el color de texto del tema. |
| `src/plugin/modelo/tipos.ts` | **Modificar.** `MensajeUI` suma `dark?`. |
| `src/plugin/main.ts` | **Modificar.** `aplicarTema` antes de generar; `finalizar` aplica el fondo. |
| `src/ui/index.html` | **Modificar.** Checkbox "Dark mode". |
| `src/ui/ui.ts` | **Modificar.** Manda `dark` con el estado del checkbox. |
| `tests/tema.test.ts` | **Nuevo.** Tests del módulo de tema. |

---

## Task 1: Módulo de tema

**Files:**
- Create: `tests/tema.test.ts`
- Create: `src/plugin/utils/tema.ts`

- [ ] **Step 1: Escribir el test que falla**

```typescript
import { test } from "node:test";
import assert from "node:assert";
import { aplicarTema, temaActual } from "../src/plugin/utils/tema.ts";

test("dark → fondo no null", () => {
  aplicarTema(true);
  assert.notEqual(temaActual().fondo, null);
});

test("light → fondo null y texto negro", () => {
  aplicarTema(false);
  assert.equal(temaActual().fondo, null);
  assert.deepEqual(temaActual().texto, { r: 0, g: 0, b: 0 });
});
```

- [ ] **Step 2: Correr para verificar que falla**

Run: `npm test`
Expected: FALLA — `Could not resolve "../src/plugin/utils/tema.ts"`.

- [ ] **Step 3: Crear `src/plugin/utils/tema.ts`**

```typescript
interface Tema {
  texto: RGB;        // color del texto generado
  fondo: RGB | null; // fondo del frame Specifications (null = transparente, light)
}

const LIGHT: Tema = { texto: { r: 0, g: 0, b: 0 }, fondo: null };
const DARK: Tema = { texto: { r: 0.95, g: 0.95, b: 0.95 }, fondo: { r: 0.12, g: 0.12, b: 0.14 } };

let actual: Tema = LIGHT;

// Setea el tema actual (light por default, dark si dark === true).
export function aplicarTema(dark: boolean): void {
  actual = dark ? DARK : LIGHT;
}

// Devuelve el tema actual.
export function temaActual(): Tema {
  return actual;
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npm test`
Expected: 2 tests nuevos PASAN (94 en total).

- [ ] **Step 5: Commit**

```bash
git add tests/tema.test.ts src/plugin/utils/tema.ts
git commit -m "feat: modulo de tema (light/dark)"
```

---

## Task 2: `texto()` aplica el color del tema

**Files:**
- Modify: `src/plugin/generadores/frames.ts`

- [ ] **Step 1: Importar `temaActual` y aplicar el color en `texto`**

En `src/plugin/generadores/frames.ts`, agregar el import al inicio:

```typescript
import { temaActual } from "../utils/tema.ts";
```

Y en la función `texto`, justo antes del `return t;`, agregar:

```typescript
  t.fills = [{ type: "SOLID", color: temaActual().texto }];
```

- [ ] **Step 2: Verificar que compila, buildea y los tests siguen verdes**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

Run: `npm run build`
Expected: `build OK → dist/code.js + dist/ui.html`.

Run: `npm test`
Expected: `pass 94`, `fail 0`.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/frames.ts
git commit -m "feat: texto aplica el color del tema"
```

---

## Task 3: `MensajeUI` dark + `finalizar` fondo + `aplicarTema` en main

**Files:**
- Modify: `src/plugin/modelo/tipos.ts`
- Modify: `src/plugin/main.ts`

- [ ] **Step 1: Agregar `dark?` a `MensajeUI`**

Reemplazar:

```typescript
export type MensajeUI = { tipo: "generar"; seccion: Seccion; nested?: boolean };
```

por:

```typescript
export type MensajeUI = { tipo: "generar"; seccion: Seccion; nested?: boolean; dark?: boolean };
```

- [ ] **Step 2: Importar el tema en `main.ts`**

Después de la línea `import { aNodoLike } from "./extraccion/adaptador.ts";`, agregar:

```typescript
import { aplicarTema, temaActual } from "./utils/tema.ts";
```

- [ ] **Step 3: Subir el alto del panel**

Reemplazar `figma.showUI(__html__, { width: 280, height: 340 });` por:

```typescript
figma.showUI(__html__, { width: 280, height: 360 });
```

- [ ] **Step 4: `finalizar` aplica el fondo del tema**

Reemplazar la función `finalizar` por:

```typescript
// Ubica el output a la derecha del nodo seleccionado, aplica el fondo del tema,
// hace foco y avisa éxito a la UI.
function finalizar(frame: FrameNode, nodo: SceneNode): void {
  const caja = nodo.absoluteBoundingBox;
  if (caja) {
    frame.x = caja.x + caja.width + 100;
    frame.y = caja.y;
  }
  const fondo = temaActual().fondo;
  frame.fills = fondo ? [{ type: "SOLID", color: fondo }] : [];
  figma.viewport.scrollAndZoomIntoView([frame]);
  responder({ tipo: "resultado", ok: true });
}
```

- [ ] **Step 5: Setear el tema al recibir el mensaje**

En el handler `figma.ui.onmessage`, después de la línea `const nodo = seleccion[0];`, agregar:

```typescript
  aplicarTema(msg.dark ?? false);
```

- [ ] **Step 6: Verificar que compila, buildea y los tests siguen verdes**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

Run: `npm run build`
Expected: `build OK → dist/code.js + dist/ui.html`.

Run: `npm test`
Expected: `pass 94`, `fail 0`.

- [ ] **Step 7: Commit**

```bash
git add src/plugin/modelo/tipos.ts src/plugin/main.ts
git commit -m "feat: finalizar aplica el fondo del tema y main setea dark"
```

---

## Task 4: Checkbox "Dark mode" en la UI

**Files:**
- Modify: `src/ui/index.html`
- Modify: `src/ui/ui.ts`

- [ ] **Step 1: Agregar el checkbox en `src/ui/index.html`**

Reemplazar la línea `<label><input type="checkbox" id="nested" /> Spec nested subcomponents</label>` por:

```html
    <label><input type="checkbox" id="nested" /> Spec nested subcomponents</label>
    <label><input type="checkbox" id="dark" /> Dark mode</label>
```

- [ ] **Step 2: Modificar `src/ui/ui.ts`**

Reemplazar el bloque de `nestedCheck` + `generar` por:

```typescript
const nestedCheck = document.getElementById("nested") as HTMLInputElement;
const darkCheck = document.getElementById("dark") as HTMLInputElement;

function generar(seccion: "anatomy" | "properties" | "layout" | "data" | "styling" | "modes" | "twoway" | "complete"): void {
  parent.postMessage({ pluginMessage: { tipo: "generar", seccion, nested: nestedCheck.checked, dark: darkCheck.checked } }, "*");
}
```

- [ ] **Step 3: Verificar que compila y buildea**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

Run: `npm run build`
Expected: `build OK → dist/code.js + dist/ui.html`.

- [ ] **Step 4: Commit**

```bash
git add src/ui/index.html src/ui/ui.ts
git commit -m "feat: checkbox Dark mode en la UI"
```

---

## Task 5: Verificación manual end-to-end en Figma

**Files:** ninguno (validación manual).

- [ ] **Step 1: Recargar el plugin**

Run: `npm run build`
En Figma: Plugins → Development → Specs Plugin. Verificar que aparece el checkbox **"Dark mode"**.

- [ ] **Step 2: Caso feliz (dark off vs on)**

- Con **Dark mode apagado** → seleccionar un componente → "Anatomy" → output igual que hoy (fondo
  transparente, texto negro).
- Con **Dark mode encendido** → "Anatomy" → output con **fondo oscuro y texto claro**; el **artwork** (el
  clon) queda con su fondo claro, sin cambios. Panel: "✓ Generado".

- [ ] **Step 3: Probar el dark en varias secciones**

Con Dark mode encendido, probar Properties / Layout / Styling / Data / Modes → todas deben salir con el
fondo oscuro y texto claro.

- [ ] **Step 4: Comparar contra la referencia del PRD**

Abrir `prd-images/11. Dark Mode/` y comparar. Anotar diferencias (marcadores/overlays tematizados, 3 niveles)
como pulido — NO arreglarlas ahora.

- [ ] **Step 5: Verificar que con dark off todo queda como antes**

Apagar Dark mode → todas las secciones salen idénticas a antes de esta rebanada.

- [ ] **Step 6: Commit de cierre (si hubo ajustes menores)**

```bash
git add -A
git commit -m "chore: verificacion end-to-end de Dark Mode en Figma"
```

---

## Resumen de cobertura del spec

| Sección del spec | Tarea(s) |
|------------------|----------|
| 1 — Módulo de tema | Task 1 |
| 2 — Aplicar el tema (texto + finalizar + UI) | Task 2 (texto), Task 3 (finalizar + main), Task 4 (checkbox) |
| 3 — Errores y casos límite | Task 1 (light = como hoy), Task 3 (fondo null en light) |
| 4 — Testing | Task 1 (unit), Task 5 (manual) |
