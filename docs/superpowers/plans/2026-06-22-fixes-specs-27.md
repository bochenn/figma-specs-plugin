# Fixes del output (Specifications-27) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Padding-top al `title`; tipo de width/height (Hug/Fill/Fixed) en cards de Anatomy; icono de tipo de capa en Hierarchy; padding del panel en 4 filas labeled.

**Architecture:** `encabezado.ts` (padding), `tipos.ts`+`atributos.ts`+`anatomy.ts` (prefijo de resizing), `recorrer.ts`+`recorrer-autolayout.ts`+`iconos.ts`+`layout.ts` (iconos en breadcrumb), `layout.ts` (helper `filasPadding`).

**Tech Stack:** TypeScript, Figma Plugin API, esbuild, `npm test`.

---

## Nota de testing

Tareas puras / con test: 2 (prefijo en atributos), 3 (cambio de `camino` en recorrer-autolayout —actualiza tests existentes). Impuras (PDF): 1 (padding-top), render de `filaAtributo` (2), iconos en breadcrumb (3), filas de padding (4). Verificación de cada tarea: `npm run build && npm test` sin regresiones. Conteo base actual: 211 tests.

---

## Task 1: Padding-top al `title` del encabezado

**Files:**
- Modify: `src/plugin/generadores/encabezado.ts`

- [ ] **Step 1: Agregar paddingTop a la barra**

En `barraStatus`, después de `barra.paddingBottom = 12;` agregá:

```ts
  barra.paddingTop = 24;
```

- [ ] **Step 2: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests sin regresiones.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/encabezado.ts
git commit -m "fix: padding-top en la barra title del encabezado"
```

---

## Task 2: Tipo de width/height en las cards de Anatomy

**Files:**
- Modify: `src/plugin/modelo/tipos.ts` (`Atributo`)
- Modify: `src/plugin/utils/atributos.ts`
- Modify: `src/plugin/generadores/anatomy.ts` (`filaAtributo`)
- Test: `tests/atributos.test.ts`

- [ ] **Step 1: Agregar `prefijo` al tipo `Atributo`**

En `src/plugin/modelo/tipos.ts`, en la interface `Atributo`, agregá el campo opcional:

```ts
export interface Atributo {
  clave: string;        // "background-color", "border-color", "width", "opacity"
  valor: string;        // hex, "Colección/Variable", o nombre del style
  formato: "HARDCODED" | "VARIABLE" | "STYLE";
  rawValue?: string;    // hex resuelto (para variable/style)
  swatchHex?: string;   // color del swatch (presente en atributos de color)
  prefijo?: string;     // modo de resizing para width/height ("Fixed" | "Hug" | "Fill")
}
```

- [ ] **Step 2: Escribir los tests que fallan**

En `tests/atributos.test.ts`, agregá al final:

```ts
test("width con layoutSizing FIXED → incluye prefijo Fixed", () => {
  const nodo: NodoLike = { id: "x", name: "x", type: "FRAME", width: 67, layoutSizingHorizontal: "FIXED" };
  assert.deepEqual(
    leerAtributos(nodo).find((a) => a.clave === "width"),
    { clave: "width", valor: "67px", formato: "HARDCODED", prefijo: "Fixed" },
  );
});

test("width con layoutSizing HUG → incluye prefijo Hug", () => {
  const nodo: NodoLike = { id: "x", name: "x", type: "FRAME", width: 88, layoutSizingHorizontal: "HUG" };
  assert.deepEqual(
    leerAtributos(nodo).find((a) => a.clave === "width"),
    { clave: "width", valor: "88px", formato: "HARDCODED", prefijo: "Hug" },
  );
});

test("width sin layoutSizing → sin prefijo (igual que antes)", () => {
  const nodo: NodoLike = { id: "x", name: "x", type: "FRAME", width: 240 };
  assert.deepEqual(
    leerAtributos(nodo).find((a) => a.clave === "width"),
    { clave: "width", valor: "240px", formato: "HARDCODED" },
  );
});
```

- [ ] **Step 3: Correr y verificar que fallan**

Run: `npm test`
Expected: FALLAN los 2 primeros (hoy no hay `prefijo`).

- [ ] **Step 4: Implementar el modo en `dimensionAtributo` + `leerAtributos`**

En `src/plugin/utils/atributos.ts`, reemplazá `dimensionAtributo`:

```ts
// Atributo de dimensión: VARIABLE (nombre + rawValue) si hay variable atada;
// HARDCODED (valor pelado) si no.
function dimensionAtributo(clave: string, px: number, nombreVar?: string): Atributo {
  const valorFmt = formatearEspaciado(px, unidadActual(), true);
  if (nombreVar) return { clave, valor: nombreVar, formato: "VARIABLE", rawValue: valorFmt };
  return { clave, valor: valorFmt, formato: "HARDCODED" };
}
```

por:

```ts
// Mapea el modo de resizing de Figma a su etiqueta ("FIXED" → "Fixed", etc.).
function modoDeSizing(s?: string): string | undefined {
  if (s === "HUG") return "Hug";
  if (s === "FILL") return "Fill";
  if (s === "FIXED") return "Fixed";
  return undefined;
}

// Atributo de dimensión: VARIABLE (nombre + rawValue) si hay variable atada;
// HARDCODED (valor pelado) si no. `modo` agrega el prefijo de resizing si está.
function dimensionAtributo(clave: string, px: number, nombreVar?: string, modo?: string): Atributo {
  const valorFmt = formatearEspaciado(px, unidadActual(), true);
  const a: Atributo = nombreVar
    ? { clave, valor: nombreVar, formato: "VARIABLE", rawValue: valorFmt }
    : { clave, valor: valorFmt, formato: "HARDCODED" };
  const prefijo = modoDeSizing(modo);
  if (prefijo) a.prefijo = prefijo;
  return a;
}
```

Y en `leerAtributos`, reemplazá las dos líneas de dimensión:

```ts
  if (typeof nodo.width === "number") {
    atributos.push(dimensionAtributo("width", nodo.width, nodo.widthVariableName));
  }

  if (typeof nodo.height === "number" && nodo.heightVariableName) {
    atributos.push(dimensionAtributo("height", nodo.height, nodo.heightVariableName));
  }
```

por:

```ts
  if (typeof nodo.width === "number") {
    atributos.push(dimensionAtributo("width", nodo.width, nodo.widthVariableName, nodo.layoutSizingHorizontal));
  }

  if (typeof nodo.height === "number" && nodo.heightVariableName) {
    atributos.push(dimensionAtributo("height", nodo.height, nodo.heightVariableName, nodo.layoutSizingVertical));
  }
```

- [ ] **Step 5: Renderizar el prefijo en `filaAtributo`**

En `src/plugin/generadores/anatomy.ts`, en `filaAtributo`, después de la línea
`nodos.push(await textoClave(`${attr.clave}:`));` agregá:

```ts
  if (attr.prefijo) nodos.push(await textoValor(attr.prefijo));
```

- [ ] **Step 6: Correr y verificar que pasan**

Run: `npm test`
Expected: PASAN los nuevos y los existentes (el test sin layoutSizing sigue sin prefijo).

- [ ] **Step 7: Commit**

```bash
git add src/plugin/modelo/tipos.ts src/plugin/utils/atributos.ts src/plugin/generadores/anatomy.ts tests/atributos.test.ts
git commit -m "feat: tipo de resizing (Hug/Fill/Fixed) en width/height de las cards"
```

---

## Task 3: Icono de tipo de capa en la columna Hierarchy

**Files:**
- Modify: `src/plugin/traversal/recorrer.ts` (`Recorrido.camino`)
- Modify: `src/plugin/traversal/recorrer-autolayout.ts`
- Modify: `tests/recorrer-autolayout.test.ts`
- Modify: `src/plugin/generadores/iconos.ts` (`nodoIconoTipo`)
- Modify: `src/plugin/generadores/layout.ts` (`breadcrumb` + call sites)

- [ ] **Step 1: Cambiar el tipo de `camino` y actualizar los tests**

En `src/plugin/traversal/recorrer.ts`, reemplazá:

```ts
export interface Recorrido { nodo: NodoLike; profundidad: number; camino?: string[]; }
```
por:
```ts
export interface Recorrido { nodo: NodoLike; profundidad: number; camino?: { nombre: string; tipo: string }[]; }
```

En `tests/recorrer-autolayout.test.ts`, reemplazá las 3 aserciones de `camino`:

```ts
  assert.deepEqual(recorrerAutoLayout(raiz)[0].camino, ["Root"]);
```
por:
```ts
  assert.deepEqual(recorrerAutoLayout(raiz)[0].camino, [{ nombre: "Root", tipo: "FRAME" }]);
```

```ts
  assert.deepEqual(recorrerAutoLayout(raiz)[0].camino, ["Root", "Inner"]);
```
por:
```ts
  assert.deepEqual(recorrerAutoLayout(raiz)[0].camino, [{ nombre: "Root", tipo: "FRAME" }, { nombre: "Inner", tipo: "FRAME" }]);
```

```ts
  assert.deepEqual(recorrerAutoLayout(raiz, true).map((r) => r.camino), [["screen"], ["screen", "card"], ["screen", "card", "tag"]]);
```
por:
```ts
  assert.deepEqual(recorrerAutoLayout(raiz, true).map((r) => r.camino), [
    [{ nombre: "screen", tipo: "FRAME" }],
    [{ nombre: "screen", tipo: "FRAME" }, { nombre: "card", tipo: "INSTANCE" }],
    [{ nombre: "screen", tipo: "FRAME" }, { nombre: "card", tipo: "INSTANCE" }, { nombre: "tag", tipo: "FRAME" }],
  ]);
```

- [ ] **Step 2: Correr y verificar que fallan**

Run: `npm test`
Expected: FALLAN los tests de camino (estructura vieja `string[]`).

- [ ] **Step 3: Construir el `camino` con tipo en `recorrerAutoLayout`**

En `src/plugin/traversal/recorrer-autolayout.ts`, reemplazá la firma y la línea de `propio`:

```ts
export function recorrerAutoLayout(nodo: NodoLike, itemizar = false, prof = 0, camino: string[] = []): Recorrido[] {
```
por:
```ts
export function recorrerAutoLayout(nodo: NodoLike, itemizar = false, prof = 0, camino: { nombre: string; tipo: string }[] = []): Recorrido[] {
```

y:
```ts
  const propio = [...camino, nodo.name];
```
por:
```ts
  const propio = [...camino, { nombre: nodo.name, tipo: nodo.type }];
```

- [ ] **Step 4: Correr y verificar que pasan los tests de camino**

Run: `npm test`
Expected: PASAN los tests de camino. (El build de `layout.ts` puede tipar mal hasta el Step 6;
si `npm run build` falla por el tipo de `breadcrumb`, seguí al Step 5/6 y revalidá.)

- [ ] **Step 5: Agregar parámetro de tamaño a `nodoIconoTipo`**

En `src/plugin/generadores/iconos.ts`, reemplazá la firma y los replace de tamaño:

```ts
export function nodoIconoTipo(tipo: string): SceneNode | undefined {
  const raw = ICONOS_TIPO[tipo];
  if (!raw) return undefined;
  const svg = raw
    .replace(/width="\d+"/, 'width="24"')
    .replace(/height="\d+"/, 'height="24"')
```
por:
```ts
export function nodoIconoTipo(tipo: string, tam = 24): SceneNode | undefined {
  const raw = ICONOS_TIPO[tipo];
  if (!raw) return undefined;
  const svg = raw
    .replace(/width="\d+"/, `width="${tam}"`)
    .replace(/height="\d+"/, `height="${tam}"`)
```

(El resto de la función queda igual.)

- [ ] **Step 6: Renderizar icono + nombre por entrada en `breadcrumb`**

En `src/plugin/generadores/layout.ts`, verificá que `nodoIconoTipo` esté importado de
`./iconos.ts` (hoy se importa `nodoIcono`; agregá `nodoIconoTipo` a ese import). Luego
reemplazá la función `breadcrumb` completa:

```ts
async function breadcrumb(camino: string[]): Promise<FrameNode> {
  const col = frameVertical("Hierarchy", 4);
  col.counterAxisSizingMode = "FIXED";
  col.resize(ANCHO_BREADCRUMB, col.height);
  for (let i = 0; i < camino.length; i++) {
    const t = await texto("  ".repeat(i) + camino[i], 12);
    if (i < camino.length - 1) t.fills = [{ type: "SOLID", color: GRIS_ANCESTRO }];
    col.appendChild(t);
  }
  return col;
}
```

por:

```ts
async function breadcrumb(camino: { nombre: string; tipo: string }[]): Promise<FrameNode> {
  const col = frameVertical("Hierarchy", 4);
  col.counterAxisSizingMode = "FIXED";
  col.resize(ANCHO_BREADCRUMB, col.height);
  for (let i = 0; i < camino.length; i++) {
    const fila = frameHorizontal("Capa", 4);
    fila.counterAxisAlignItems = "CENTER";
    fila.paddingLeft = i * 12;
    const icono = nodoIconoTipo(camino[i].tipo, 16);
    if (icono) fila.appendChild(icono);
    const t = await texto(camino[i].nombre, 12);
    if (i < camino.length - 1) t.fills = [{ type: "SOLID", color: GRIS_ANCESTRO }];
    fila.appendChild(t);
    col.appendChild(fila);
  }
  return col;
}
```

- [ ] **Step 7: Actualizar los call sites de `breadcrumb`**

En `layout.ts`, reemplazá:

```ts
    fila.appendChild(await breadcrumb(recorridos[i].camino ?? [specs[i].elementoNombre]));
```
por:
```ts
    fila.appendChild(await breadcrumb(recorridos[i].camino ?? [{ nombre: specs[i].elementoNombre, tipo: specs[i].tipo }]));
```

y:
```ts
      fila.appendChild(await breadcrumb([seleccionado.name]));
```
por:
```ts
      fila.appendChild(await breadcrumb([{ nombre: seleccionado.name, tipo: seleccionado.type }]));
```

- [ ] **Step 8: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests pasan (incluidos los de camino).

- [ ] **Step 9: Commit**

```bash
git add src/plugin/traversal/recorrer.ts src/plugin/traversal/recorrer-autolayout.ts tests/recorrer-autolayout.test.ts src/plugin/generadores/iconos.ts src/plugin/generadores/layout.ts
git commit -m "feat: icono de tipo de capa por elemento en la columna Hierarchy"
```

---

## Task 4: Padding del panel en 4 filas etiquetadas

**Files:**
- Modify: `src/plugin/generadores/layout.ts` (`exhibit` + nuevo helper `filasPadding`)

- [ ] **Step 1: Agregar el helper `filasPadding`**

En `src/plugin/generadores/layout.ts`, justo antes de la función `exhibit`, agregá:

```ts
// Filas de padding del panel: una sola "Padding" si los 4 lados son uniformes; si no,
// una fila por lado (Top/Right/Bottom/Left).
async function filasPadding(p: LayoutSpec["padding"], sv: LayoutSpec["spacingVars"], u: Unidad): Promise<FrameNode[]> {
  const uniforme = p.left === p.top && p.top === p.right && p.right === p.bottom
    && sv.paddingLeft === sv.paddingTop && sv.paddingTop === sv.paddingRight && sv.paddingRight === sv.paddingBottom;
  if (uniforme) return [await filaPropiedad("padding", "Padding", valorSpacing(p.left, u, sv.paddingLeft))];
  return [
    await filaPropiedad("padding", "Padding top", valorSpacing(p.top, u, sv.paddingTop)),
    await filaPropiedad("padding", "Padding right", valorSpacing(p.right, u, sv.paddingRight)),
    await filaPropiedad("padding", "Padding bottom", valorSpacing(p.bottom, u, sv.paddingBottom)),
    await filaPropiedad("padding", "Padding left", valorSpacing(p.left, u, sv.paddingLeft)),
  ];
}
```

Verificá que `Unidad` esté importado en `layout.ts` (viene de `../modelo/tipos.ts` o de
`../utils/espaciado.ts`). Si no lo está, agregá `import type { Unidad } from "../modelo/tipos.ts";`.

- [ ] **Step 2: Quitar el bloque `partesPadding` del `exhibit`**

En `exhibit`, eliminá este bloque completo:

```ts
  // Padding: chip si los 4 lados comparten variable y valor; si no, texto colapsado.
  const p = spec.padding;
  const padUniforme = p.left === p.top && p.top === p.right && p.right === p.bottom && sv.paddingLeft === sv.paddingTop && sv.paddingTop === sv.paddingRight && sv.paddingRight === sv.paddingBottom;
  const partesPadding: ParteValor[] = padUniforme
    ? valorSpacing(p.left, u, sv.paddingLeft)
    : [
        ...valorSpacing(p.top, u, sv.paddingTop),
        ...valorSpacing(p.right, u, sv.paddingRight),
        ...valorSpacing(p.bottom, u, sv.paddingBottom),
        ...valorSpacing(p.left, u, sv.paddingLeft),
      ];
```

- [ ] **Step 3: Usar `filasPadding` en las dos ramas del `exhibit`**

En la rama GRID, reemplazá:

```ts
    filas.push(await filaPropiedad("padding", "Padding", partesPadding));
```
por:
```ts
    filas.push(...await filasPadding(spec.padding, sv, u));
```

En la rama auto-layout (más abajo), reemplazá la otra ocurrencia idéntica:

```ts
  filas.push(await filaPropiedad("padding", "Padding", partesPadding));
```
por:
```ts
  filas.push(...await filasPadding(spec.padding, sv, u));
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK (sin `partesPadding` ni `ParteValor` sin uso —`ParteValor` sigue usándose en `filaPropiedad`/`valorConChips`, así que su import se mantiene); tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "feat: padding del panel en 4 filas etiquetadas (Top/Right/Bottom/Left)"
```

---

## Verificación final (manual, por PDF)

Generar Anatomy + Layout y exportar PDF; confirmar:
- Aire arriba de la barra del encabezado.
- Cards de Anatomy: `width: Fixed 67px`, `height: Hug …`, etc.
- Hierarchy: cada capa con su icono de tipo (frame/instancia/texto/grupo…).
- Padding no uniforme: 4 filas `Padding top/right/bottom/left`; uniforme: una fila `Padding`.
