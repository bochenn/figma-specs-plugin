# Fixes del output (Specifications-26) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aplicar 7 fixes al output: renombrar `_Status`→`title`, quitar prefijo `N. ` de variables, omitir cota de hijo=padre, redondear a 2 decimales, `px` en el panel, padding como chipVar por lado.

**Architecture:** Cambios por archivo: `encabezado.ts` (rename), nuevo `utils/nombre-variable.ts` + `adaptador.ts` (prefijo), `utils/espaciado.ts` (redondeo + unidad), `utils/marcadores-layout.ts` + `utils/atributos.ts` (px en panel), `generadores/layout.ts` (padding chips + omitir hijo=padre).

**Tech Stack:** TypeScript, Figma Plugin API, esbuild, `npm test` (node --test sobre `tests/*.test.ts`).

---

## Nota de testing

Tareas puras con test: 2 (prefijo), 3 (redondeo + unidad), 4 (px en panel — actualiza tests
existentes). Tareas impuras (verificación por PDF, sin test nuevo): 1 (rename), 5 (padding
chips), 6 (omitir hijo=padre). Verificación de cada tarea: `npm run build && npm test` sin
errores ni regresiones.

---

## Task 1: Renombrar el layer `_Status` → `title`

**Files:**
- Modify: `src/plugin/generadores/encabezado.ts`

- [ ] **Step 1: Cambiar el nombre del frame**

En `barraStatus`, reemplazá:

```ts
  const barra = frameHorizontal("_Status", 0);
```

por:

```ts
  const barra = frameHorizontal("title", 0);
```

- [ ] **Step 2: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests sin regresiones.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/encabezado.ts
git commit -m "fix: renombrar layer _Status a title en el encabezado"
```

---

## Task 2: Quitar el prefijo `N. ` del nombre de las variables

**Files:**
- Create: `src/plugin/utils/nombre-variable.ts`
- Create: `tests/nombre-variable.test.ts`
- Modify: `src/plugin/extraccion/adaptador.ts`

- [ ] **Step 1: Escribir el test que falla**

Creá `tests/nombre-variable.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert";
import { limpiarPrefijoColeccion } from "../src/plugin/utils/nombre-variable.ts";

test("quita el prefijo 'N. ' inicial", () => {
  assert.equal(limpiarPrefijoColeccion("1. Color modes"), "Color modes");
  assert.equal(limpiarPrefijoColeccion("3. Spacing"), "Spacing");
  assert.equal(limpiarPrefijoColeccion("10. Tokens"), "Tokens");
});

test("no toca nombres sin prefijo numérico", () => {
  assert.equal(limpiarPrefijoColeccion("Color modes"), "Color modes");
  assert.equal(limpiarPrefijoColeccion("Spacing/2xl"), "Spacing/2xl");
});
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `npm test`
Expected: FALLA (módulo `nombre-variable.ts` no existe).

- [ ] **Step 3: Crear el helper**

Creá `src/plugin/utils/nombre-variable.ts`:

```ts
// Quita un prefijo de numeración inicial tipo "N. " del nombre (ej. el de una
// colección de variables: "1. Color modes" → "Color modes").
export function limpiarPrefijoColeccion(nombre: string): string {
  return nombre.replace(/^\d+\.\s*/, "");
}
```

- [ ] **Step 4: Correr y verificar que pasa**

Run: `npm test`
Expected: PASAN los 2 tests nuevos y los existentes.

- [ ] **Step 5: Aplicarlo en el adaptador**

En `src/plugin/extraccion/adaptador.ts`, agregá el import (junto a los otros del tope):

```ts
import { limpiarPrefijoColeccion } from "../utils/nombre-variable.ts";
```

Y en `nombreVariable`, reemplazá:

```ts
  return col ? `${col.name}/${variable.name}` : variable.name;
```

por:

```ts
  return col ? `${limpiarPrefijoColeccion(col.name)}/${variable.name}` : variable.name;
```

- [ ] **Step 6: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests pasan.

- [ ] **Step 7: Commit**

```bash
git add src/plugin/utils/nombre-variable.ts tests/nombre-variable.test.ts src/plugin/extraccion/adaptador.ts
git commit -m "fix: quitar prefijo 'N. ' del nombre de las variables"
```

---

## Task 3: Redondear a 2 decimales + unidad opcional en `formatearEspaciado`

**Files:**
- Modify: `src/plugin/utils/espaciado.ts`
- Test: `tests/espaciado.test.ts`

- [ ] **Step 1: Escribir los tests que fallan**

En `tests/espaciado.test.ts`, agregá al final:

```ts
test("formatearEspaciado redondea a máximo 2 decimales", () => {
  assert.equal(formatearEspaciado(485.3333435058594, "px"), "485.33");
  assert.equal(formatearEspaciado(197.33334350585938, "px"), "197.33");
  assert.equal(formatearEspaciado(296, "px"), "296");
});

test("formatearEspaciado con mostrarUnidad agrega px", () => {
  assert.equal(formatearEspaciado(296, "px", true), "296px");
  assert.equal(formatearEspaciado(485.3333435058594, "px", true), "485.33px");
  assert.equal(formatearEspaciado(24, "rem", true), "1.5rem");
});
```

- [ ] **Step 2: Correr y verificar que fallan**

Run: `npm test`
Expected: FALLAN los nuevos (hoy devuelve el número crudo y no acepta el 3er parámetro).

- [ ] **Step 3: Implementar redondeo + parámetro**

En `src/plugin/utils/espaciado.ts`, reemplazá:

```ts
// Formatea un valor en px al formato elegido (px = número pelado; rem = n/16).
export function formatearEspaciado(n: number, unidad: Unidad): string {
  return unidad === "rem" ? `${n / 16}rem` : String(n);
}
```

por:

```ts
// Redondea a máximo 2 decimales (sin ceros sobrantes).
function redondear2(n: number): number {
  return Math.round(n * 100) / 100;
}

// Formatea un valor en px al formato elegido. `mostrarUnidad` agrega "px" (rem siempre
// lleva su unidad). px sin unidad = número pelado (para las cotas del artwork).
export function formatearEspaciado(n: number, unidad: Unidad, mostrarUnidad = false): string {
  if (unidad === "rem") return `${redondear2(n / 16)}rem`;
  const v = redondear2(n);
  return mostrarUnidad ? `${v}px` : `${v}`;
}
```

- [ ] **Step 4: Correr y verificar que pasan**

Run: `npm test`
Expected: PASAN los nuevos y los existentes (los que usan px sin unidad siguen dando el número pelado).

- [ ] **Step 5: Commit**

```bash
git add src/plugin/utils/espaciado.ts tests/espaciado.test.ts
git commit -m "fix: redondear medidas a 2 decimales y unidad px opcional"
```

---

## Task 4: Mostrar `px` en el panel/cards

**Files:**
- Modify: `src/plugin/utils/marcadores-layout.ts` (`valorDim`, `valorSpacing`)
- Modify: `src/plugin/utils/atributos.ts` (`dimensionAtributo`)
- Test: `tests/marcadores-layout.test.ts`, `tests/atributos.test.ts`

- [ ] **Step 1: Actualizar los tests al nuevo formato con px**

En `tests/marcadores-layout.test.ts`, reemplazá:

```ts
  assert.deepEqual(valorDim("Fixed", 240, "px", "sizing/card-width"), [{ texto: "Fixed" }, { chip: "sizing/card-width" }, { texto: "(240)" }]);
```
por:
```ts
  assert.deepEqual(valorDim("Fixed", 240, "px", "sizing/card-width"), [{ texto: "Fixed" }, { chip: "sizing/card-width" }, { texto: "(240px)" }]);
```

reemplazá:
```ts
  assert.deepEqual(valorDim("Hug", 88, "px"), [{ texto: "Hug 88" }]);
```
por:
```ts
  assert.deepEqual(valorDim("Hug", 88, "px"), [{ texto: "Hug 88px" }]);
```

reemplazá:
```ts
  assert.deepEqual(valorSpacing(16, "px", "space/padding-1x"), [{ chip: "space/padding-1x" }, { texto: "(16)" }]);
```
por:
```ts
  assert.deepEqual(valorSpacing(16, "px", "space/padding-1x"), [{ chip: "space/padding-1x" }, { texto: "(16px)" }]);
```

reemplazá:
```ts
  assert.deepEqual(valorSpacing(8, "px"), [{ texto: "8" }]);
```
por:
```ts
  assert.deepEqual(valorSpacing(8, "px"), [{ texto: "8px" }]);
```

En `tests/atributos.test.ts`, reemplazá:

```ts
    { clave: "width", valor: "240", formato: "HARDCODED" },
```
por:
```ts
    { clave: "width", valor: "240px", formato: "HARDCODED" },
```

reemplazá:
```ts
    { clave: "width", valor: "DS Sizing/iOS width", formato: "VARIABLE", rawValue: "343" },
```
por:
```ts
    { clave: "width", valor: "DS Sizing/iOS width", formato: "VARIABLE", rawValue: "343px" },
```

reemplazá:
```ts
    { clave: "height", valor: "DS Sizing/button", formato: "VARIABLE", rawValue: "48" },
```
por:
```ts
    { clave: "height", valor: "DS Sizing/button", formato: "VARIABLE", rawValue: "48px" },
```

- [ ] **Step 2: Correr y verificar que fallan**

Run: `npm test`
Expected: FALLAN esos tests (hoy devuelven sin px).

- [ ] **Step 3: Pasar `mostrarUnidad = true` en los formatters del panel**

En `src/plugin/utils/marcadores-layout.ts`, reemplazá `valorDim`:

```ts
export function valorDim(resizing: string, px: number, unidad: Unidad, nombreVar?: string): ParteValor[] {
  if (nombreVar) return [{ texto: resizing }, { chip: nombreVar }, { texto: `(${formatearEspaciado(px, unidad)})` }];
  return [{ texto: `${resizing} ${formatearEspaciado(px, unidad)}` }];
}
```
por:
```ts
export function valorDim(resizing: string, px: number, unidad: Unidad, nombreVar?: string): ParteValor[] {
  if (nombreVar) return [{ texto: resizing }, { chip: nombreVar }, { texto: `(${formatearEspaciado(px, unidad, true)})` }];
  return [{ texto: `${resizing} ${formatearEspaciado(px, unidad, true)}` }];
}
```

y `valorSpacing`:

```ts
export function valorSpacing(px: number, unidad: Unidad, nombreVar?: string): ParteValor[] {
  if (nombreVar) return [{ chip: nombreVar }, { texto: `(${formatearEspaciado(px, unidad)})` }];
  return [{ texto: formatearEspaciado(px, unidad) }];
}
```
por:
```ts
export function valorSpacing(px: number, unidad: Unidad, nombreVar?: string): ParteValor[] {
  if (nombreVar) return [{ chip: nombreVar }, { texto: `(${formatearEspaciado(px, unidad, true)})` }];
  return [{ texto: formatearEspaciado(px, unidad, true) }];
}
```

En `src/plugin/utils/atributos.ts`, en `dimensionAtributo`, reemplazá:

```ts
  const valorFmt = formatearEspaciado(px, unidadActual());
```
por:
```ts
  const valorFmt = formatearEspaciado(px, unidadActual(), true);
```

- [ ] **Step 4: Correr y verificar que pasan**

Run: `npm test`
Expected: PASAN todos (los actualizados con px y el resto). Nota: `textoDimension` y las cotas
NO cambian (siguen usando `etiquetaSpacing`, sin px); sus tests quedan igual.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/utils/marcadores-layout.ts src/plugin/utils/atributos.ts tests/marcadores-layout.test.ts tests/atributos.test.ts
git commit -m "fix: mostrar unidad px en los valores del panel/cards"
```

---

## Task 5: Padding no uniforme como un chipVar por lado

**Files:**
- Modify: `src/plugin/generadores/layout.ts` (`exhibit`)

- [ ] **Step 1: Reemplazar la rama no uniforme por partes por lado**

En `exhibit`, reemplazá:

```ts
  const partesPadding: ParteValor[] = padUniforme ? valorSpacing(p.left, u, sv.paddingLeft) : [{ texto: textoPadding(p, u, sv) }];
```

por:

```ts
  const partesPadding: ParteValor[] = padUniforme
    ? valorSpacing(p.left, u, sv.paddingLeft)
    : [
        ...valorSpacing(p.top, u, sv.paddingTop),
        ...valorSpacing(p.right, u, sv.paddingRight),
        ...valorSpacing(p.bottom, u, sv.paddingBottom),
        ...valorSpacing(p.left, u, sv.paddingLeft),
      ];
```

Nota: queda sin usar el import `textoPadding` (de `./espaciado.ts`) en `layout.ts`. Quitalo
de la línea de import de `espaciado.ts` para que no quede import sin uso. (La función
`textoPadding` y sus tests se mantienen; solo deja de usarse acá.)

- [ ] **Step 2: Build + tests**

Run: `npm run build && npm test`
Expected: build OK (sin import sin uso); tests sin regresiones.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "fix: padding no uniforme como un chipVar por lado en el panel"
```

---

## Task 6: Omitir la cota de un hijo cuando coincide con el padre

**Files:**
- Modify: `src/plugin/generadores/layout.ts` (`dibujarMarcas`, `dibujarLineasHijos`, y su llamada)

- [ ] **Step 1: Saltear en `dibujarMarcas` las cotas de hijo iguales al padre**

En `dibujarMarcas`, reemplazá el loop de medidas de hijos:

```ts
  const u = unidadActual();
  for (const h of hijos) {
    top.push({ c: await cota(etiquetaSpacing(h.width, u), COTA_DIM, artwork), centro: h.x + h.width / 2 });
    left.push({ c: await cota(etiquetaSpacing(h.height, u), COTA_DIM, artwork), centro: h.y + h.height / 2 });
  }
```

por (omite ancho si coincide con el del clon, alto si coincide con el del clon):

```ts
  const u = unidadActual();
  for (const h of hijos) {
    if (h.width !== clon.width) top.push({ c: await cota(etiquetaSpacing(h.width, u), COTA_DIM, artwork), centro: h.x + h.width / 2 });
    if (h.height !== clon.height) left.push({ c: await cota(etiquetaSpacing(h.height, u), COTA_DIM, artwork), centro: h.y + h.height / 2 });
  }
```

- [ ] **Step 2: Saltear las líneas/brackets correspondientes en `dibujarLineasHijos`**

Reemplazá la función completa:

```ts
function dibujarLineasHijos(artwork: FrameNode, hijos: Rect[]): void {
  for (const h of hijos) {
    const cw = figma.createNodeFromSvg(svgCotaH("fixed", h.width));
    cw.x = h.x;
    cw.y = h.y - 14;
    artwork.appendChild(cw);
    const ch = figma.createNodeFromSvg(svgCotaV("fixed", h.height));
    ch.x = h.x - 14;
    ch.y = h.y;
    artwork.appendChild(ch);
  }
}
```

por (recibe el ancho/alto del padre y omite la línea de la dimensión que coincide):

```ts
function dibujarLineasHijos(artwork: FrameNode, hijos: Rect[], anchoPadre: number, altoPadre: number): void {
  for (const h of hijos) {
    if (h.width !== anchoPadre) {
      const cw = figma.createNodeFromSvg(svgCotaH("fixed", h.width));
      cw.x = h.x;
      cw.y = h.y - 14;
      artwork.appendChild(cw);
    }
    if (h.height !== altoPadre) {
      const ch = figma.createNodeFromSvg(svgCotaV("fixed", h.height));
      ch.x = h.x - 14;
      ch.y = h.y;
      artwork.appendChild(ch);
    }
  }
}
```

- [ ] **Step 3: Actualizar la llamada a `dibujarLineasHijos`**

En `artworkModo`, reemplazá:

```ts
  if (medirHijos && modo !== "spacing") dibujarLineasHijos(artwork, hijosRects);
```

por:

```ts
  if (medirHijos && modo !== "spacing") dibujarLineasHijos(artwork, hijosRects, clon.width, clon.height);
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "fix: omitir la cota de un hijo cuando coincide con la del padre"
```

---

## Verificación final (manual, por PDF)

Generar Anatomy + Layout y exportar PDF; confirmar:
- Encabezado: el layer interno se llama `title`.
- Variables sin prefijo `N. ` (ej. `Color modes/...`, `Spacing/spacing-2xl`).
- Medidas con máximo 2 decimales (no `485.3333435…`).
- `px` en el panel/cards (ej. `(296px)`, `Width: Fixed chip (296px)`); cotas del artwork sin px.
- Padding no uniforme: un chipVar por lado.
- En Dimensions, ya no se repite la medida del padre por cada hijo full-width.
