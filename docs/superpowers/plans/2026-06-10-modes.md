# Modes — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mostrar, para las variables de color vinculadas a la selección, cómo cambian sus valores entre los modes de cada variable collection (≥2 modes), en una sección textual disparada por un sexto botón "Modes".

**Architecture:** Primera feature que toca variables. Un recolector impure (`variables/recolectar-modes.ts`, usa `figma.variables`) produce `EntradaModo[]` desde el nodo real. La agrupación (`agruparModes`) y el formato de color (`hexDeColor`) son puros y testeados. El generador `generadores/modes.ts` pivotea por mode. La UI suma un botón; `main.ts` agrega la rama `modes`.

**Tech Stack:** TypeScript, esbuild, `@figma/plugin-typings`, `node --test`. Sin frameworks de UI.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---------|-----------------|
| `src/plugin/modelo/tipos.ts` | **Modificar.** Agrega `ValorModo`, `EntradaModo`, `AtributoModo`, `ColeccionModes`; suma `"modes"` a `Seccion`. |
| `src/plugin/variables/modes.ts` | **Nuevo.** `hexDeColor`, `agruparModes`. Lógica pura. |
| `src/plugin/variables/recolectar-modes.ts` | **Nuevo.** `recolectarModes(nodo) → EntradaModo[]`. Impure (figma.variables). |
| `src/plugin/generadores/modes.ts` | **Nuevo.** `generarModes(nombre, colecciones)`. Toca `figma.*`. |
| `src/ui/index.html` | **Modificar.** Sexto botón "Modes". |
| `src/ui/ui.ts` | **Modificar.** Binding del botón modes. |
| `src/plugin/main.ts` | **Modificar.** Rama `modes`. |
| `tests/hex-de-color.test.ts` | **Nuevo.** Tests de `hexDeColor`. |
| `tests/agrupar-modes.test.ts` | **Nuevo.** Tests de `agruparModes`. |

---

## Task 1: Tipos de Modes

**Files:**
- Modify: `src/plugin/modelo/tipos.ts`

- [ ] **Step 1: Sumar `"modes"` a `Seccion`**

Reemplazar `export type Seccion = "anatomy" | "properties" | "layout" | "data" | "styling";` por:

```typescript
export type Seccion = "anatomy" | "properties" | "layout" | "data" | "styling" | "modes";
```

- [ ] **Step 2: Agregar los tipos de Modes al final del archivo**

```typescript
// --- Modes ---

export interface ValorModo {
  modeId: string;
  valor: string;   // hex o "→ nombreAlias"
}

export interface EntradaModo {
  coleccionNombre: string;
  modos: { modeId: string; nombre: string }[];
  capa: string;
  appliedAs: string;
  variableNombre: string;
  valores: ValorModo[];
}

export interface AtributoModo {
  capa: string;
  appliedAs: string;
  variableNombre: string;
  valores: ValorModo[];
}

export interface ColeccionModes {
  coleccionNombre: string;
  modos: { modeId: string; nombre: string }[];
  atributos: AtributoModo[];
}
```

- [ ] **Step 3: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

- [ ] **Step 4: Commit**

```bash
git add src/plugin/modelo/tipos.ts
git commit -m "feat: tipos de Modes (EntradaModo, ColeccionModes, seccion modes)"
```

---

## Task 2: `hexDeColor`

**Files:**
- Create: `tests/hex-de-color.test.ts`
- Create: `src/plugin/variables/modes.ts`

- [ ] **Step 1: Escribir el test que falla**

```typescript
import { test } from "node:test";
import assert from "node:assert";
import { hexDeColor } from "../src/plugin/variables/modes.ts";

test("blanco → #FFFFFF", () => {
  assert.equal(hexDeColor({ r: 1, g: 1, b: 1 }), "#FFFFFF");
});

test("negro → #000000", () => {
  assert.equal(hexDeColor({ r: 0, g: 0, b: 0 }), "#000000");
});

test("gris medio → #808080", () => {
  assert.equal(hexDeColor({ r: 0.5, g: 0.5, b: 0.5 }), "#808080");
});
```

- [ ] **Step 2: Correr para verificar que falla**

Run: `npm test`
Expected: FALLA — `Could not resolve "../src/plugin/variables/modes.ts"`.

- [ ] **Step 3: Crear `src/plugin/variables/modes.ts` con `hexDeColor`**

```typescript
// Convierte un color (canales 0..1) a hex #RRGGBB en mayúsculas.
export function hexDeColor(rgb: { r: number; g: number; b: number }): string {
  const canal = (n: number) => Math.round(n * 255).toString(16).padStart(2, "0").toUpperCase();
  return "#" + canal(rgb.r) + canal(rgb.g) + canal(rgb.b);
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npm test`
Expected: 3 tests nuevos PASAN (55 en total).

- [ ] **Step 5: Commit**

```bash
git add tests/hex-de-color.test.ts src/plugin/variables/modes.ts
git commit -m "feat: hexDeColor (color 0..1 -> hex)"
```

---

## Task 3: `agruparModes`

**Files:**
- Create: `tests/agrupar-modes.test.ts`
- Modify: `src/plugin/variables/modes.ts`

- [ ] **Step 1: Escribir el test que falla**

```typescript
import { test } from "node:test";
import assert from "node:assert";
import { agruparModes } from "../src/plugin/variables/modes.ts";
import type { EntradaModo } from "../src/plugin/modelo/tipos.ts";

const MODOS = [{ modeId: "L", nombre: "Light" }, { modeId: "D", nombre: "Dark" }];

function entrada(coleccion: string, appliedAs: string, variableNombre: string): EntradaModo {
  return {
    coleccionNombre: coleccion,
    modos: MODOS,
    capa: "Alert",
    appliedAs,
    variableNombre,
    valores: [{ modeId: "L", valor: "#FFFFFF" }, { modeId: "D", valor: "#000000" }],
  };
}

test("dos entradas de la misma collection → una ColeccionModes con dos atributos", () => {
  const cols = agruparModes([
    entrada("Color", "Background color", "Bg"),
    entrada("Color", "Border color", "Bd"),
  ]);
  assert.equal(cols.length, 1);
  assert.equal(cols[0].coleccionNombre, "Color");
  assert.deepEqual(cols[0].modos, MODOS);
  assert.equal(cols[0].atributos.length, 2);
  assert.equal(cols[0].atributos[0].variableNombre, "Bg");
  assert.equal(cols[0].atributos[1].variableNombre, "Bd");
});

test("entradas de dos collections → dos ColeccionModes, en orden de aparición", () => {
  const cols = agruparModes([
    entrada("Color", "Background color", "Bg"),
    entrada("Spacing", "Border color", "Sp"),
  ]);
  assert.deepEqual(cols.map((c) => c.coleccionNombre), ["Color", "Spacing"]);
});
```

- [ ] **Step 2: Correr para verificar que falla**

Run: `npm test`
Expected: FALLA — `agruparModes is not a function` / export no encontrado.

- [ ] **Step 3: Agregar `agruparModes` a `src/plugin/variables/modes.ts`**

Agregar el import al inicio del archivo:

```typescript
import type { EntradaModo, ColeccionModes } from "../modelo/tipos.ts";
```

Y la función al final:

```typescript
// Agrupa las entradas por collection (orden de primera aparición); toma los
// modos de la primera entrada de cada collection.
export function agruparModes(entradas: EntradaModo[]): ColeccionModes[] {
  const orden: string[] = [];
  const grupos = new Map<string, ColeccionModes>();
  for (const e of entradas) {
    let g = grupos.get(e.coleccionNombre);
    if (!g) {
      orden.push(e.coleccionNombre);
      g = { coleccionNombre: e.coleccionNombre, modos: e.modos, atributos: [] };
      grupos.set(e.coleccionNombre, g);
    }
    g.atributos.push({
      capa: e.capa,
      appliedAs: e.appliedAs,
      variableNombre: e.variableNombre,
      valores: e.valores,
    });
  }
  return orden.map((n) => grupos.get(n)!);
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npm test`
Expected: 2 tests nuevos PASAN (57 en total).

- [ ] **Step 5: Commit**

```bash
git add tests/agrupar-modes.test.ts src/plugin/variables/modes.ts
git commit -m "feat: agruparModes (entradas -> ColeccionModes)"
```

---

## Task 4: Recolector impure `recolectarModes`

Lee las variables de color vinculadas y resuelve sus valores por mode. Toca `figma.variables`; se valida compilando y en la verificación manual.

**Files:**
- Create: `src/plugin/variables/recolectar-modes.ts`

- [ ] **Step 1: Implementar `src/plugin/variables/recolectar-modes.ts`**

```typescript
import type { EntradaModo, ValorModo } from "../modelo/tipos.ts";
import { hexDeColor } from "./modes.ts";

// Variables de color vinculadas a fill/stroke de un nodo, con su appliedAs.
function variablesColorDe(nodo: SceneNode): { id: string; appliedAs: string }[] {
  const res: { id: string; appliedAs: string }[] = [];
  if (!("boundVariables" in nodo) || !nodo.boundVariables) return res;
  const bv = nodo.boundVariables as {
    fills?: readonly VariableAlias[];
    strokes?: readonly VariableAlias[];
  };
  if (bv.fills && bv.fills.length > 0) {
    res.push({ id: bv.fills[0].id, appliedAs: nodo.type === "TEXT" ? "Text color" : "Background color" });
  }
  if (bv.strokes && bv.strokes.length > 0) {
    res.push({ id: bv.strokes[0].id, appliedAs: "Border color" });
  }
  return res;
}

// Formatea el valor de una variable de color en un mode: hex, alias o "—".
function valorColor(variable: Variable, modeId: string): string {
  const raw = variable.valuesByMode[modeId];
  if (raw === undefined || raw === null) return "—";
  if (typeof raw === "object" && "type" in raw && raw.type === "VARIABLE_ALIAS") {
    const alias = figma.variables.getVariableById(raw.id);
    return alias ? `→ ${alias.name}` : "→ (alias)";
  }
  if (typeof raw === "object" && "r" in raw) {
    return hexDeColor(raw);
  }
  return "—";
}

// Visita un nodo: emite entradas de sus variables de color y baja por sus hijos
// (salvo en instancias).
function visitar(nodo: SceneNode, entradas: EntradaModo[]): void {
  for (const { id, appliedAs } of variablesColorDe(nodo)) {
    const variable = figma.variables.getVariableById(id);
    if (!variable || variable.resolvedType !== "COLOR") continue;
    const collection = figma.variables.getVariableCollectionById(variable.variableCollectionId);
    if (!collection || collection.modes.length < 2) continue;

    const modos = collection.modes.map((m) => ({ modeId: m.modeId, nombre: m.name }));
    const valores: ValorModo[] = modos.map((m) => ({ modeId: m.modeId, valor: valorColor(variable, m.modeId) }));
    entradas.push({
      coleccionNombre: collection.name,
      modos,
      capa: nodo.name,
      appliedAs,
      variableNombre: variable.name,
      valores,
    });
  }
  if (nodo.type === "INSTANCE") return;
  if ("children" in nodo) {
    for (const hijo of nodo.children) visitar(hijo, entradas);
  }
}

// Recolecta las entradas de modes de la selección (raíz + descendientes).
export function recolectarModes(nodo: SceneNode): EntradaModo[] {
  const entradas: EntradaModo[] = [];
  visitar(nodo, entradas);
  return entradas;
}
```

- [ ] **Step 2: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/variables/recolectar-modes.ts
git commit -m "feat: recolectarModes (variables de color vinculadas, valores por mode)"
```

---

## Task 5: Generador de Modes

**Files:**
- Create: `src/plugin/generadores/modes.ts`

- [ ] **Step 1: Implementar `src/plugin/generadores/modes.ts`**

```typescript
import type { ColeccionModes } from "../modelo/tipos.ts";
import { frameVertical, texto } from "./frames.ts";

// Bloque de un mode: su nombre + los atributos con su valor en ese mode.
async function bloqueMode(modeId: string, nombre: string, coleccion: ColeccionModes): Promise<FrameNode> {
  const bloque = frameVertical(nombre, 8);
  bloque.appendChild(await texto(nombre, 24));
  for (const attr of coleccion.atributos) {
    const v = attr.valores.find((x) => x.modeId === modeId);
    const valor = v ? v.valor : "—";
    bloque.appendChild(await texto(`${attr.appliedAs}: ${attr.variableNombre} (${valor})`, 12));
  }
  return bloque;
}

// Subsección de una collection: heading + un bloque por mode.
async function subseccionColeccion(coleccion: ColeccionModes): Promise<FrameNode> {
  const sub = frameVertical(coleccion.coleccionNombre, 40);
  sub.appendChild(await texto(coleccion.coleccionNombre, 36));
  for (const modo of coleccion.modos) {
    sub.appendChild(await bloqueMode(modo.modeId, modo.nombre, coleccion));
  }
  return sub;
}

// Genera el output de Modes. Devuelve el frame Specifications.
export async function generarModes(nombre: string, colecciones: ColeccionModes[]): Promise<FrameNode> {
  const specifications = frameVertical("Specifications", 128, 64);
  const spec = frameVertical(`${nombre} Spec`, 48);
  const seccion = frameVertical("Modes", 64);

  specifications.appendChild(spec);
  spec.appendChild(await texto(nombre, 64));
  spec.appendChild(seccion);
  seccion.appendChild(await texto("Modes", 48));

  if (colecciones.length === 0) {
    seccion.appendChild(await texto("No se detectaron variables con múltiples modes.", 16));
  }
  for (const c of colecciones) {
    seccion.appendChild(await subseccionColeccion(c));
  }

  figma.currentPage.appendChild(specifications);
  return specifications;
}
```

- [ ] **Step 2: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/modes.ts
git commit -m "feat: generador de Modes (subseccion por collection, bloque por mode)"
```

---

## Task 6: Sexto botón en la UI

**Files:**
- Modify: `src/ui/index.html`
- Modify: `src/ui/ui.ts`

- [ ] **Step 1: Agregar el botón en `src/ui/index.html`**

Reemplazar la línea `<button id="styling">Styling Inventory</button>` por:

```html
    <button id="styling">Styling Inventory</button>
    <button id="modes">Modes</button>
```

- [ ] **Step 2: Modificar `src/ui/ui.ts`**

Cambiar la firma de `generar`:

```typescript
function generar(seccion: "anatomy" | "properties" | "layout" | "data" | "styling" | "modes"): void {
```

Y después de la línea `(document.getElementById("styling") as HTMLButtonElement).onclick = () => generar("styling");`, agregar:

```typescript
(document.getElementById("modes") as HTMLButtonElement).onclick = () => generar("modes");
```

- [ ] **Step 3: Verificar que compila y buildea**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

Run: `npm run build`
Expected: `build OK → dist/code.js + dist/ui.html`.

- [ ] **Step 4: Commit**

```bash
git add src/ui/index.html src/ui/ui.ts
git commit -m "feat: sexto boton Modes en la UI"
```

---

## Task 7: Rama `modes` en main.ts

**Files:**
- Modify: `src/plugin/main.ts`

- [ ] **Step 1: Agregar los imports de Modes**

En `src/plugin/main.ts`, después de la línea `import { generarStyling } from "./generadores/styling.ts";`, agregar:

```typescript
import { recolectarModes } from "./variables/recolectar-modes.ts";
import { agruparModes } from "./variables/modes.ts";
import { generarModes } from "./generadores/modes.ts";
```

- [ ] **Step 2: Subir el alto del panel**

Reemplazar `figma.showUI(__html__, { width: 280, height: 260 });` por:

```typescript
figma.showUI(__html__, { width: 280, height: 280 });
```

- [ ] **Step 3: Agregar la función `generarSeccionModes`**

Después de la función `generarSeccionStyling` completa (antes de `figma.ui.onmessage`), agregar:

```typescript
async function generarSeccionModes(nodo: SceneNode): Promise<void> {
  if (!TIPOS_VALIDOS.includes(nodo.type)) {
    responder({ tipo: "resultado", ok: false, error: "Modes necesita un FRAME, COMPONENT, INSTANCE o COMPONENT_SET." });
    return;
  }
  const colecciones = agruparModes(recolectarModes(nodo));
  const frame = await generarModes(nodo.name, colecciones);
  finalizar(frame, nodo);
}
```

- [ ] **Step 4: Agregar la rama en el dispatcher**

Reemplazar:

```typescript
    else await generarSeccionStyling(nodo);
```

por:

```typescript
    else if (msg.seccion === "styling") await generarSeccionStyling(nodo);
    else await generarSeccionModes(nodo);
```

- [ ] **Step 5: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: `TypeScript compilation completed`.

- [ ] **Step 6: Build y tests**

Run: `npm run build`
Expected: `build OK → dist/code.js + dist/ui.html`.

Run: `npm test`
Expected: `pass 57`, `fail 0`.

- [ ] **Step 7: Commit**

```bash
git add src/plugin/main.ts
git commit -m "feat: rama modes en main (Modes)"
```

---

## Task 8: Verificación manual end-to-end en Figma

**Files:** ninguno (validación manual).

- [ ] **Step 1: Preparar una variable collection de color con 2 modes**

En Figma: crear una variable collection (ej. "Color") con **dos modes** (ej. "Light" y "Dark"). Crear
variables de color (ej. "Background" y "Border") con valores distintos en cada mode. Aplicarlas al **fill**
y **stroke** de unas capas dentro de un frame contenedor.

- [ ] **Step 2: Recargar el plugin**

Run: `npm run build`
En Figma: Plugins → Development → Specs Plugin. Verificar que el panel muestra **seis botones**.

- [ ] **Step 3: Caso feliz (Modes)**

Seleccionar el frame contenedor → botón **"Modes"**.
Expected: aparece `Specifications → [Nombre] Spec → Modes` con una subsección por la collection (ej.
"Color"), y dentro un bloque por cada mode (Light, Dark), cada uno listando los atributos con su valor hex
en ese mode (ej. `Background color: Background (#EDF0F8)`). El output aparece a la derecha del frame
seleccionado (no superpuesto). Panel: "✓ Generado".

- [ ] **Step 4: Comparar contra la referencia del PRD**

Abrir `prd-images/7. Modes/` y comparar la estructura. Anotar diferencias (artwork por mode) como pulido
para la rebanada siguiente — NO arreglarlas ahora.

- [ ] **Step 5: Verificar que los otros botones siguen funcionando y casos límite**

- Anatomy / Properties / Layout / Data / Styling desde sus botones.
- Frame **sin variables de color con múltiples modes** → "Modes" → "No se detectaron variables con múltiples modes."

- [ ] **Step 6: Commit de cierre (si hubo ajustes menores)**

```bash
git add -A
git commit -m "chore: verificacion end-to-end de Modes en Figma"
```

---

## Resumen de cobertura del spec

| Sección del spec | Tarea(s) |
|------------------|----------|
| 1 — Lectura de variables y recolección | Task 1 (tipos), Task 4 (recolectarModes) |
| 2 — Agrupación pura y formato | Task 2 (hexDeColor), Task 3 (agruparModes) |
| 3 — Generador y disparador | Task 5 (generador), Task 6 (UI), Task 7 (main rama modes) |
| 4 — Errores y casos límite | Task 4 (saltea sin resolver), Task 5 (sin colecciones), Task 7 (validación/try-catch) |
| 5 — Testing | Tasks 2–3 (unit), Task 8 (manual) |
