# Fixes del output (Specifications-30) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) o superpowers:executing-plans. Steps usan checkbox (`- [ ]`).

**Goal:** Texto medium en marcadores/cotas; párrafo explicativo por sección; marcadores distribuidos por lados con líneas cortas; cota de gap pegada al gap.

**Architecture:** `frames.ts` (FONT_MEDIUM + parrafoSeccion), `anatomy.ts` (font medium + párrafo + marcadores), `layout.ts` (font medium + párrafo + gap in-place).

**Tech Stack:** TypeScript, Figma Plugin API, esbuild, `npm test`.

---

## Nota de testing

Todo impuro (figma.*), sin tests nuevos. Verificación de cada tarea: `npm run build && npm test`
sin errores ni regresiones (214 tests).

---

## Task 1: FONT_MEDIUM en marcadores y cotas

**Files:**
- Modify: `src/plugin/generadores/frames.ts`
- Modify: `src/plugin/generadores/anatomy.ts`
- Modify: `src/plugin/generadores/layout.ts`

- [ ] **Step 1: Definir `FONT_MEDIUM` en frames.ts**

En `src/plugin/generadores/frames.ts`, después de `export const FONT_SEMI ...`, agregá:

```ts
// Inter Medium (con fallback a Regular vía cargarFont).
export const FONT_MEDIUM: FontName[] = [{ family: "Inter", style: "Medium" }, { family: "Inter", style: "Regular" }];
```

- [ ] **Step 2: Usar FONT_MEDIUM en los números de marcador (anatomy.ts)**

En `src/plugin/generadores/anatomy.ts`, agregá `FONT_MEDIUM` al import desde `./frames.ts`.
En `marcador`, reemplazá:
```ts
  const num = await texto(String(numero), 14);
```
por:
```ts
  const num = await texto(String(numero), 14, FONT_MEDIUM);
```
En `badgePanel`, reemplazá:
```ts
  const num = await texto(String(numero), 11);
```
por:
```ts
  const num = await texto(String(numero), 11, FONT_MEDIUM);
```

- [ ] **Step 3: Usar FONT_MEDIUM en los textos de cota (layout.ts)**

En `src/plugin/generadores/layout.ts`, agregá `FONT_MEDIUM` al import desde `./frames.ts`.
En `cota`, reemplazá:
```ts
  const t = await texto(valor, 11);
```
por:
```ts
  const t = await texto(valor, 11, FONT_MEDIUM);
```
En `cotaConNombre`, reemplazá:
```ts
  const tn = await texto(nombre, 11);
```
por:
```ts
  const tn = await texto(nombre, 11, FONT_MEDIUM);
```
y reemplazá:
```ts
  const tv = await texto(valor, 11);
```
por:
```ts
  const tv = await texto(valor, 11, FONT_MEDIUM);
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; 214 tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/frames.ts src/plugin/generadores/anatomy.ts src/plugin/generadores/layout.ts
git commit -m "feat: textos de marcadores y cotas en weight medium"
```

---

## Task 2: Párrafo explicativo bajo el tag (Anatomy y Layout)

**Files:**
- Modify: `src/plugin/generadores/frames.ts`
- Modify: `src/plugin/generadores/anatomy.ts`
- Modify: `src/plugin/generadores/layout.ts`

- [ ] **Step 1: Helper `parrafoSeccion` en frames.ts**

En `src/plugin/generadores/frames.ts`, después de `tagSeccion`, agregá:

```ts
// Párrafo descriptivo de una sección (gris, ancho fijo con wrap). Va debajo del tag.
export async function parrafoSeccion(descripcion: string, ancho = 720): Promise<TextNode> {
  const t = await texto(descripcion, 14);
  t.fills = [{ type: "SOLID", color: COLOR_CLAVE }];
  t.textAutoResize = "HEIGHT";
  t.resize(ancho, t.height);
  return t;
}
```

(`COLOR_CLAVE` = #6B7280 ya existe en frames.ts.)

- [ ] **Step 2: Párrafo en Anatomy**

En `anatomy.ts`, agregá `parrafoSeccion` al import desde `./frames.ts`. En `seccionDeAnatomy`,
reemplazá:
```ts
  const seccion = frameVertical("Anatomy", 24);
  seccion.appendChild(await tagSeccion("Anatomy"));
```
por:
```ts
  const seccion = frameVertical("Anatomy", 24);
  seccion.appendChild(await tagSeccion("Anatomy"));
  seccion.appendChild(await parrafoSeccion("Desglosa el elemento en sus capas. Cada capa se numera sobre el diseño (a la izquierda) y se detalla a la derecha con su tipo y sus atributos —color, dimensiones, tipografía y las variables aplicadas—. Úsalo para entender de qué está compuesto el elemento y qué tokens del sistema usa cada parte."));
```

- [ ] **Step 3: Párrafo en Layout**

En `layout.ts`, agregá `parrafoSeccion` al import desde `./frames.ts`. En `seccionDeLayout`,
reemplazá:
```ts
  seccion.appendChild(await tagSeccion("Layout and Spacing"));
```
por:
```ts
  seccion.appendChild(await tagSeccion("Layout and Spacing"));
  seccion.appendChild(await parrafoSeccion("Muestra cómo se organiza el contenido: dirección, alineación, padding, espaciado entre ítems (gap) y dimensiones de cada frame con Auto Layout. Las cotas sobre el diseño marcan las medidas en su lugar; el panel de la derecha las detalla con sus variables. Úsalo para reproducir el espaciado y el comportamiento de redimensionado."));
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; 214 tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/frames.ts src/plugin/generadores/anatomy.ts src/plugin/generadores/layout.ts
git commit -m "feat: párrafo explicativo bajo el tag de Anatomy y Layout"
```

---

## Task 3: Marcadores distribuidos por lados con líneas cortas

**Files:**
- Modify: `src/plugin/generadores/anatomy.ts` (`seccionDeAnatomy` + helper)

- [ ] **Step 1: Agregar `lineaGuiaH` junto a `lineaGuiaV`**

En `anatomy.ts`, después de `lineaGuiaV`, agregá:

```ts
// Línea guía horizontal recta (del color del marcador): de `desdeX` a `haciaX` en la Y dada.
function lineaGuiaH(artwork: FrameNode, desdeX: number, y: number, haciaX: number, color: RGB): void {
  const h = figma.createRectangle();
  h.x = Math.min(desdeX, haciaX);
  h.y = y - 0.5;
  h.resize(Math.max(Math.abs(haciaX - desdeX), 0.01), 1);
  h.fills = [{ type: "SOLID", color }];
  artwork.appendChild(h);
}
```

- [ ] **Step 2: Reservar margen para marcadores y centrar el clon**

En `seccionDeAnatomy`, reemplazá el bloque del clon/canvas:

```ts
  // Margen para que los badges de las capas pegadas al borde no se corten.
  const MARGEN_ARTWORK = 20;
  // Margen superior (riel) donde se ubican los marcadores (callouts) sobre el clon.
  const RIEL_TOP = 64;
  // Tamaño mínimo del canvas gris: un elemento chico queda centrado en una caja amplia.
  const ARTWORK_MIN = 440;
  const clon = seleccionado.clone();
  artwork.appendChild(clon);
  const canvasW = Math.max(ARTWORK_MIN, clon.width + 2 * MARGEN_ARTWORK);
  const areaH = Math.max(ARTWORK_MIN, clon.height + 2 * MARGEN_ARTWORK);
  const canvasH = areaH + RIEL_TOP;
  artwork.resize(canvasW, canvasH);
  const offsetX = (canvasW - clon.width) / 2;
  const offsetY = RIEL_TOP + (areaH - clon.height) / 2;
  clon.x = offsetX;
  clon.y = offsetY;
```

por:

```ts
  // Margen para que los badges de las capas pegadas al borde no se corten.
  const MARGEN_ARTWORK = 20;
  // Distancia del marcador al borde de su box, y margen reservado para los marcadores.
  const OFFSET_MARCA = 48;
  const MARGEN_MARCA = OFFSET_MARCA + TAM_MARCADOR;
  // Tamaño mínimo del canvas gris: un elemento chico queda centrado en una caja amplia.
  const ARTWORK_MIN = 440;
  const clon = seleccionado.clone();
  artwork.appendChild(clon);
  const canvasW = Math.max(ARTWORK_MIN, clon.width + 2 * (MARGEN_ARTWORK + MARGEN_MARCA));
  const canvasH = Math.max(ARTWORK_MIN, clon.height + 2 * (MARGEN_ARTWORK + MARGEN_MARCA));
  artwork.resize(canvasW, canvasH);
  const offsetX = (canvasW - clon.width) / 2;
  const offsetY = (canvasH - clon.height) / 2;
  clon.x = offsetX;
  clon.y = offsetY;
```

- [ ] **Step 3: Reemplazar el loop de marcadores (lado izq por defecto, arriba si colisiona)**

Reemplazá el bloque actual de borde + marcadores (el que arma `anchors`, hace `sort` y el loop
del riel superior) por:

```ts
  // Cada marcador va JUSTO AFUERA de su box con una línea recta corta:
  // por defecto a la izquierda (línea horizontal); si colisiona con otro ya puesto, arriba
  // (línea vertical). El número es el índice del elemento (coincide con la lista).
  const cajas = cajasRelativas(seleccionado);
  const colocados: { x: number; y: number }[] = [];
  const colisiona = (cx: number, cy: number): boolean =>
    colocados.some((m) => Math.abs(m.x - cx) < TAM_MARCADOR + 4 && Math.abs(m.y - cy) < TAM_MARCADOR + 4);
  for (let i = 0; i < elementos.length; i++) {
    const caja = cajas.get(elementos[i].id);
    if (!caja) continue;
    const color = COLORES_MARCA[i % COLORES_MARCA.length];
    const bx = caja.x + offsetX;
    const by = caja.y + offsetY;
    bordeMarca({ x: bx, y: by, width: caja.width, height: caja.height }, color, artwork);

    // Lado izquierdo (default): centro del marcador a la izquierda del box, en su centro vertical.
    let mcx = bx - OFFSET_MARCA - TAM_MARCADOR / 2;
    let mcy = by + caja.height / 2;
    if (colisiona(mcx, mcy)) {
      // Lado superior: centro del marcador arriba del box, en su centro horizontal.
      mcx = bx + caja.width / 2;
      mcy = by - OFFSET_MARCA - TAM_MARCADOR / 2;
      lineaGuiaV(artwork, mcx, mcy + TAM_MARCADOR / 2, by, color);
    } else {
      lineaGuiaH(artwork, mcx + TAM_MARCADOR / 2, mcy, bx, color);
    }
    artwork.appendChild(await marcador(i + 1, mcx - TAM_MARCADOR / 2, mcy - TAM_MARCADOR / 2, color));
    colocados.push({ x: mcx, y: mcy });
  }
```

- [ ] **Step 4: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; 214 tests sin regresiones.

- [ ] **Step 5: Commit**

```bash
git add src/plugin/generadores/anatomy.ts
git commit -m "fix: marcadores de Anatomy distribuidos por lados (izq/arriba) con líneas cortas"
```

---

## Task 4: Cota de gap pegada al gap

**Files:**
- Modify: `src/plugin/generadores/layout.ts` (`dibujarSpacingCallouts`)

- [ ] **Step 1: Reemplazar el dibujado de gaps por medición en su lugar**

En `dibujarSpacingCallouts`, reemplazá el loop de gaps:

```ts
  for (const g of gaps) {
    const val = spec.direccion === "VERTICAL" ? g.height : g.width;
    const chip = spec.spacingAuto ? await cota("Auto", COTA_GAP, artwork) : await chipSpacing(val, COTA_GAP, artwork, sv.itemSpacing);
    if (spec.direccion === "VERTICAL") vertical(g.height, g.y, LINEA_GAP, chip);
    else horizontal(g.width, g.x, LINEA_GAP, chip);
  }
```

por (gap medido en su lugar: bracket en el gap + chip adyacente, afuera del elemento):

```ts
  // Gap medido en su lugar: bracket sobre el gap + chip adyacente (no en el carril del borde).
  // Dirección HORIZONTAL → gap = franja vertical, se mide el ancho con bracket horizontal
  // ARRIBA del gap y chip encima. Dirección VERTICAL → gap = franja horizontal, se mide el alto
  // con bracket vertical a la DERECHA del gap y chip al lado.
  const gapHorizontal = (g: Rect, chip: FrameNode) => {
    const yBracket = g.y - 12;
    const br = figma.createNodeFromSvg(svgCotaH("fixed", g.width, LINEA_GAP));
    br.x = g.x; br.y = yBracket - 6; artwork.appendChild(br);
    chip.x = g.x + g.width / 2 - chip.width / 2;
    chip.y = yBracket - 6 - SEP_VALOR - chip.height;
  };
  const gapVertical = (g: Rect, chip: FrameNode) => {
    const xBracket = g.x + g.width + 12;
    const br = figma.createNodeFromSvg(svgCotaV("fixed", g.height, LINEA_GAP));
    br.x = xBracket - 6; br.y = g.y; artwork.appendChild(br);
    chip.x = xBracket + 6 + SEP_VALOR;
    chip.y = g.y + g.height / 2 - chip.height / 2;
  };
  for (const g of gaps) {
    const val = spec.direccion === "VERTICAL" ? g.height : g.width;
    const chip = spec.spacingAuto ? await cota("Auto", COTA_GAP, artwork) : await chipSpacing(val, COTA_GAP, artwork, sv.itemSpacing);
    if (spec.direccion === "VERTICAL") gapVertical(g, chip);
    else gapHorizontal(g, chip);
  }
```

(Las closures `vertical`/`horizontal` siguen usándose para el padding; no se tocan.)

- [ ] **Step 2: Build + tests**

Run: `npm run build && npm test`
Expected: build OK; 214 tests sin regresiones.

- [ ] **Step 3: Commit**

```bash
git add src/plugin/generadores/layout.ts
git commit -m "fix: cota de gap pegada al gap (medición en su lugar)"
```

---

## Verificación final (manual, por PDF)

Generar Anatomy + Layout y exportar PDF; confirmar:
- Marcadores sin superposición: el 1 a la izquierda (línea horizontal), el 2 arriba (línea vertical); líneas cortas.
- Cota de gap pegada al gap (chip arriba/al lado del gap, no en el borde del elemento).
- Números de marcadores y textos de cotas en weight medium.
- Párrafo explicativo gris debajo del tag de Anatomy y de Layout.
