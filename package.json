{
  "name": "blueprint-specs-handoff",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "bochenn",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node esbuild.config.mjs --test && node --test dist-test/*.test.js"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "@types/node": "^25.9.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
